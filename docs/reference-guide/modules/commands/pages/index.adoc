= Commands
:page-aliases: README.adoc

Commands are messages that express the intent to change application state.
They represent actions that _should_ happen, such as "create an order," "update a user profile," or "approve a payment."
In Axon Framework, commands are a fundamental building block of your application architecture.

== Command-centric approach

Axon Framework takes a **messaging-centric** approach to application design.
Commands are central to how your application processes user actions and external events.
The framework provides comprehensive support for:

* **Dispatching commands** - Sending commands from controllers, APIs, or other handlers
* **Handling commands** - Processing commands to validate requests and apply changes
* **Routing commands** - Directing commands to the appropriate handler based on routing keys
* **Coordinating changes** - Managing state changes across one or more components

This approach emphasizes _what you want to accomplish_ (commands) rather than _how you structure your domain model_.

== Command handlers: stateless and stateful

Command handlers fall into two categories:

* **Stateless command handlers**: No prior state is needed to process the command.
Each invocation is independent—for example, dispatching a batch of sub-commands, sending a notification, or forwarding a command to an external system.
See xref:commands:command-handlers.adoc[Command handlers].

* **Stateful command handlers**: Prior state is required to enforce business rules.
A course enrollment command needs the current student count to enforce capacity.
A bank account command needs the current balance to prevent overdrafts.
State is held in an event-sourced entity that Axon loads automatically before invoking the handler.
See xref:commands:entities/index.adoc[Entities].

An entity is one way to provide that state, but it is an implementation choice, not an architectural requirement.

== Subsections

[cols="<,<"]
|===
|Sub-Section |Purpose

|xref:command-handlers.adoc[Handling] |How to write stateless command handlers: parameters, message types, event appending, and configuration
|xref:command-dispatchers.adoc[Dispatching] |How to dispatch commands from your application code
|xref:entities/index.adoc[Entities] |How to maintain state across commands and enforce business rules using event-sourced entities
|xref:infrastructure.adoc[Infrastructure] |Command bus implementations, repositories, and other infrastructure components
|xref:configuration.adoc[Configuration] |How to configure command handling in your application
|===
